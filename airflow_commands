apt-get install python3
python --version
python3
python3 -V
pip3 install --upgrade pip
apt install python3-pip
apt-get update
pip3
apt install python3-pip
pip3 install --upgrade pip
pip3 install --user virtualenv
python3 -m venv airflow
apt-get install python3-venv
pip3 install --user virtualenv
python3 -m venv airflow
source airflow/bin/activate
airflow
pip3 install "apache-airflow[celery,crypto,postgres,rabbitmq,redis]==1.10.6"
airflow
pip3 install werkzeug==0.16.0
pip3 uninstall SQLAlchemy
pip3 install SQLAlchemy==1.3.15
useradd -ms /bin/bash -d /root/airflow/ airflow
cat /etc/passwd | grep airflow
chown airflow -R /root/airflow/
chgrp -R airflow /root/airflow/
source airflow/bin/activate
airflow scheduler &> /dev/null &
airflow webserver &> /dev/null &

